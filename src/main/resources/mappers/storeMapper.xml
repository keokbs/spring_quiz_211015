<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.lesson02.dao.StoreDAO">
	 <select id="selectStoreList" resultType="com.quiz.lesson02.model.Store">
	 	SELECT
	 		`id`
			,`name` 
			,`phoneNumber` 
			,`address` 
			,`businessNumber` 
			,`introduce`
			,`createdAt`
			,`updatedAt`
	 	FROM
	 		`store`;
	 </select>
	 
	 <select id="selectStoreIdByName" resultType="int">
	 	SELECT
	 		`id`
	 	FROM
	 		`store`
	 	WHERE
	 		`id` = #{id};
	 </select>
	 
	 <select id="selectNewReviewByStoreId" resultType="com.quiz.lesson05.model.NewReview">
	 	SELECT
	 		`id`
	 		,`storeId`
	 		,`menu`
	 		,`userName`
	 		,`point`
	 		,`review`
	 		,`createdAt`
	 		,`updatedAt`
	 	FROM
	 		`new_review`
	 	WHERE
	 		`storeId` = #{storeId};
	 </select>
 
 </mapper>